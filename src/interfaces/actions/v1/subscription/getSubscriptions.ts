import { UserActionArguments } from '@diia-inhouse/types'

import { SubscriptionsResponse } from '@interfaces/services/subscription'

export type CustomActionArguments = UserActionArguments

export type ActionResult = SubscriptionsResponse
